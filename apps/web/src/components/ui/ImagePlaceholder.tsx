import { cn } from '@/utils/cn';

// this is placeholder for image of this app which will be captured once app pages are completed
const ImagePlaceholder = () => {
  return <div className={cn('bg-gray-400 w-full h-80 rounded-xl')}></div>;
};

export default ImagePlaceholder;
